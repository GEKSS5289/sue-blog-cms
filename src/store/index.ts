import { createStore } from 'vuex'
import {PathModel} from "@/common/interface/datamodel";

export default createStore({
  state: {
    // path:[
    //   {name:'sue',router:'/admin/sue'},
    // ]
  },
  mutations: {
    // addPath(state,paths){
    //   state.path.push(paths)
    //   localStorage.setItem(paths.name,paths.router)
    // }
  },
  actions: {
  },
  modules: {
  }
})
