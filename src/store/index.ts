import { createStore } from 'vuex'
import {PathModel} from "@/common/model/datamodel";

export default createStore({
  state: {
    // path:[
    //   {name:'sue',api-router:'/admin/sue'},
    // ]
  },
  mutations: {
    // addPath(state,paths){
    //   state.path.push(paths)
    //   localStorage.setItem(paths.name,paths.api-router)
    // }
  },
  actions: {
  },
  modules: {
  }
})
